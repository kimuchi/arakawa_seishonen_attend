# ユーザーマニュアル

荒川区青少年委員連絡会 出席簿 (Web版) の利用方法をまとめます。

> このマニュアルはアプリケーションの **使い方** のみを扱います。サーバの設置やデプロイは `DEPLOYMENT.md` を参照してください。

---

## 0. アクセス方法

管理者から共有された URL をブラウザで開きます。
ヘッダ右上の「📄 シートを開く」を押すと、データ本体のスプレッドシートを別タブで開けます。

> 初回アクセス時、まだ設定が未完了の場合はセットアップ画面に転送されます。管理者が `/setup` から認証情報・スプレッドシートIDを登録するまで、この画面が表示されます。

---

## 1. 画面の構成

ヘッダ + 6つのタブで構成されています。

| タブ | 主にやること |
|---|---|
| **ダッシュボード** | これからのイベント・最近のイベント・年度サマリの確認 |
| **出席登録** | イベントを選び、メンバごとに〇/×を付ける |
| **イベント管理** | イベントの追加・編集・削除・ICS一括取込 |
| **メンバ管理** | メンバの追加・編集・削除 |
| **集計** | 期間別の出席数・日当額の集計、CSV出力 |
| **設定** | 年度・日当単価などのアプリ設定、分類マスタ、ICS取込ルール |

---

## 2. ダッシュボード

- 「登録イベント数 / メンバ数 / 出席記録 合計 / 日当総額」のサマリ
- 「📅 これからのイベント」「🕘 最近のイベント」の一覧
- 各イベントの **「このイベントで出欠登録」** ボタンを押すと、出席登録タブにジャンプ

---

## 3. 出席登録

### 3.1 イベントの選択

セレクトボックスから対象イベントを選びます。表示は `日付 イベント名 [分類/サブ分類] 💰`(💰は日当対象の意)。

### 3.2 一括操作

- 「全員出席」: 現在のフィルタ結果に対して、全員「〇」をセット
- 「全員クリア」: 現在のフィルタ結果に対して、状態を空にする

### 3.3 個別の出欠

- 「出席」列のセルを押すと〇に、もう一度押すと空に
- 「欠席」列のセルを押すと×に、もう一度押すと空に
- どちらに変更しても **保存ボタンを押すまでサーバには反映されません**

### 3.4 フィルタ

- 地区 / 実践部会 / 専門部会 のプルダウン
- 氏名検索 (部分一致)

### 3.5 保存

「💾 保存」を押すと、画面に表示されている全員分の状態がサーバ(スプレッドシート)に書き込まれます。

> 保存時、空の状態にしたセルは出席シートから行ごと削除されます。

---

## 4. イベント管理

### 4.1 新規イベントの追加

「+ 新規イベント」ボタン → モーダルで以下を入力:

- 日付 (必須)
- 開始時刻 / 終了時刻 (任意)
- 終日チェック
- イベント名 (必須)
- 分類 (例: ブロック / 実践部会 / 全体事業 など) と サブ分類
- 日当対象チェック (分類で選んだサブ分類のデフォルト値が自動セット)
- 場所 / 備考

「追加」を押すと保存され、一覧に反映されます。

### 4.2 編集 / 削除

各イベント行右側の「編集 / 削除」ボタンから。**削除すると関連する出席記録もまとめて削除されます。**

### 4.3 ICS取込

カレンダー (Google カレンダーの秘密 iCal URL など) を一括取込できます。

- 「🔄 ICSを1年分取込」 → 設定の「年度開始日〜年度終了日」の範囲
- 「📥 ICSを2年分取込」 → 上記+さらに1年先まで

> **取込前の準備**: ICS取込URL は 設定タブ右下「🔧 接続設定 (config.json 編集)」または `/setup` 画面で登録してください。
>
> **重複防止**: 同日・同名のイベントは取り込まれません。Google カレンダーのイベント UID も照合します。

### 4.4 重複イベントの掃除

「🧹 重複イベントを掃除」ボタン → 既に登録済みの重複イベントを 1 件残して削除。

---

## 5. メンバ管理

メンバの追加・編集・削除を行います。スプレッドシート(メンバシート)から直接編集することもできますが、画面操作のほうが整合性が保てるのでこちらを推奨。

> 既存メンバの「氏名 / 役職」は、年度途中で変わった場合も自由に変更できます。**メンバを削除すると、そのメンバの出席記録もまとめて削除されます。** 残したい場合は、シート上で「有効」列を `FALSE` に切り替えれば一覧から外せます。

---

## 6. 集計

### 6.1 期間の選択

- 2年度通算 (設定年度＋次年度)
- 設定年度 (年度開始日 ～ 年度終了日)
- 次年度 (年度開始日+1年 ～ 年度終了日+1年)
- 1年目 上半期 / 下半期
- 2年目 上半期 / 下半期
- カスタム (任意の開始日・終了日)

「集計実行」ボタンで再計算します(期間を変えたら毎回押してください)。

### 6.2 表示内容

- **メンバ別**: 出席回数、日当対象出席、金額
- **ブロック別**: 地区ごとの日当合計
- **対象期間 / 対象イベント数 / 日当総額** のサマリ

### 6.3 CSV出力

「CSVダウンロード」ボタンで、メンバ別 + ブロック別合計を UTF-8 BOM 付き CSV としてダウンロードできます。Excelでそのまま開けます。

---

## 7. 設定

### 7.1 アプリ設定

- 年度 / 年度開始日 / 年度終了日 / 上半期終了日 / 日当単価 / 組織名

各行右端の「保存」を押すと反映されます。

### 7.2 分類マスタ

イベントの分類(大)とサブ分類の組み合わせを管理。
↑↓ で表示順入替、💾 で保存、🗑 で削除。**「日当」 のチェックは、今後追加するイベントの日当対象デフォルト** として使われます (既存イベントの日当対象フラグは変わりません)。

### 7.3 ICS取込ルール

ICS取込時、イベント名に含まれるキーワードから分類を自動推定するルール。

- パターン: 文字列 (またはマッチタイプ=正規表現)
- マッチ: 「含む」 / 「正規表現」
- 分類 / サブ分類: ヒットしたら割り当てる値
- 日当 (デフォルト): ヒット時の日当対象フラグの初期値
- 有効 / 無効

**評価順は上から順** で、最初にヒットしたルールが適用されます。下のテスト機能で実際にどう分類されるか確認できます。

### 7.4 管理ボタン

- 📄 スプレッドシートを開く
- 🔄 データ再読込
- 🛠️ シート再構築 (シートが消えた・古い形式の場合に補完)
- 🔧 接続設定 (config.json 編集) → 認証情報・スプレッドシート ID・ICS取込URL の更新画面

---

## 8. データの保管場所

- 全データは **Google スプレッドシート** に保存されています (Web 版アプリはあくまでスプレッドシートの操作インターフェース)
- スプレッドシートの URL はヘッダ右上「📄 シートを開く」から
- 必要に応じて Google ドライブの「バージョン履歴」で過去状態に戻せます

---

## 9. よくある質問

**Q. 同時に複数人が操作しても大丈夫?**
→ A. 出席登録・イベント編集など、書き込み系の操作はサーバ側でロックして直列に実行されます。たまたま同じイベントの同じメンバを同時に変更した場合、後勝ちになります。

**Q. スマホからでも使えますか?**
→ A. はい。レスポンシブ対応です。出席登録は片手でも操作しやすいよう、〇/× ボタンを大きく表示します。

**Q. データが消えたらどうすれば?**
→ A. スプレッドシートの「バージョン履歴」(ファイル → バージョン履歴) から過去のスナップショットに戻せます。

**Q. 年度切替の運用は?**
→ A. 2年1期なら、設定の「年度」を今期の開始年に固定したまま、集計タブで「2年度通算」を選ぶと累計が見られます。新しい期にしたい場合は、新規スプレッドシートを作って `config.json` を切り替えるのがおすすめ(旧期はそのまま保管されます)。

**Q. ログイン機構が無いと心配です**
→ A. 共有 URL を限定したグループ(LINEグループ等)でのみ共有し、外部に漏らさない運用が前提です。組織でアカウント認証を被せたい場合は、デプロイガイドの「セキュリティ上の注意」を参照してください。

---

## 10. 連絡先

不具合・要望があれば、運用担当者(管理者)までお知らせください。
